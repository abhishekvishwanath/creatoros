fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
anthropic==0.42.0
groq==1.7.0
pgvector==0.3.6
python-dotenv==1.0.1
httpx==0.28.1
pyjwt==2.10.1
# Required by PyJWT for ES256/RS256 verification — the default signing
# algorithm for new Supabase projects' access tokens (see app/api/deps.py).
cryptography==44.0.0

# Semantic memory (CLAUDE.md §6.2): local ONNX embedding model, no API key
# or torch needed — see app/agent_service/memory/embeddings.py.
fastembed==0.8.0
# YouTube link ingestion (see app/domain/ingestion/youtube.py): fetches
# public auto-generated caption tracks for transcript-based voice analysis.
youtube-transcript-api==1.2.4

pytest==8.3.4
pytest-asyncio==0.25.0

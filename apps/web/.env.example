# FastAPI backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (Project Settings -> API). Leave blank to run in dev-mode auth
# (a local placeholder identity stored in the browser) until Supabase Auth
# is wired up in the frontend.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
